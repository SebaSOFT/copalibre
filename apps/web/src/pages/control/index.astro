---
const returnTo = Astro.url.searchParams.get('returnTo');
const target = new URL('/control/login', Astro.url);
if (returnTo) target.searchParams.set('returnTo', returnTo);
return Astro.redirect(target.toString(), 302);
---
