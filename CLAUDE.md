# CopaLibre

Read [AGENTS.md](./AGENTS.md). It is the single source for this repository's instructions —
structure, commands, architecture, domain language, tests and contracts, CI, tooling, and the
feature-delivery pipeline.

This file exists only so harnesses that look for `CLAUDE.md` find their way there. Nothing is
duplicated here on purpose: a second copy is a second thing to forget to update. Anything worth
saying to an agent about this repo belongs in `AGENTS.md`, which every harness we use can read.
